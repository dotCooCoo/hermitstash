{
  "version": "1.14.26",
  "date": "2026-08-18",
  "headline": "Escape link URLs in outgoing email, and enforce the chunk-staging path checks",
  "summary": "Three defensive changes. None of them is reachable by an attacker in this release; each is a place where a later change could have made one so, or where a wrong request was answered as though it were a different one.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "A malformed chunk identifier is refused rather than reported as a missing chunk",
          "body": "Staging a chunked upload validates the bundle and file identifiers, and the chunk index, before touching the filesystem. Two of those calls sat inside the error handling that exists to tolerate a chunk which has already been removed, so the refusal was swallowed and the caller was told the chunk did not exist. A client waiting on that answer waits for a chunk that can never arrive."
        },
        {
          "title": "An absent chunk index no longer means the first chunk",
          "body": "The index was converted to a number before its type was checked, and null, an empty string, false and an empty array all convert to zero — so a request naming no index operated on chunk zero. Numbers and numeric strings are both still accepted, because the index arrives as a string on the request; anything else is refused."
        }
      ]
    },
    {
      "heading": "Security",
      "items": [
        {
          "title": "Link URLs in outgoing email are escaped for attribute context",
          "body": "The button in every message — verify your address, reset your password, accept an invite, view your upload — placed its URL straight into a quoted HTML attribute. Every one of those URLs is built from the configured origin plus a server-generated token, and the origin is read from configuration rather than the request's Host header, so nothing an attacker supplies reaches it today. The value now goes through the attribute-context escaper regardless, so a quote or a backtick can no longer end the attribute. An escaped URL decodes back to exactly what was passed in, so links are unchanged for recipients."
        }
      ]
    }
  ]
}
