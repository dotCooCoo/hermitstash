{
  "version": "1.14.24",
  "date": "2026-08-16",
  "headline": "Stop the CA regeneration asking for certificates it does not touch",
  "summary": "Regenerating the mutual-TLS CA reported that every browser certificate had been invalidated and had to be re-downloaded. Browser certificates are issued by a separate CA and are unaffected, which the documentation has always said. Following the panel instead cost a re-issue and a re-import for every browser certificate in the deployment.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "A CA regeneration no longer asks for browser certificates to be replaced",
          "body": "Two certificate authorities run here: the sync CA that issues post-quantum client certificates, and a separate classical CA for browser certificates that today's keystores can import. Regenerating the sync CA leaves browser certificates valid — it does not delete or supersede them. Five places said otherwise: the warning before you confirm, the description on the panel, the description that replaces it once the panel loads, the summary shown afterwards, and the notice printed at the next boot. Two of them also read a count that nothing writes, so the panel reported \"Browser certs invalidated: undefined\". Offline sync clients still need to re-enroll, which is unchanged."
        },
        {
          "title": "A damaged regeneration notice is reported as damaged",
          "body": "The record written before the restart is read back at boot to print what happened. A truncated or incomplete record still produced a notice — \"regenerated at undefined (v undefined → v undefined)\" — which reads like a real one and carries nothing to act on. Boot now requires the timestamp and both generation numbers before printing the notice, and reports an unreadable record as unreadable, with the file path and what to check."
        },
        {
          "title": "Release gates no longer answer from a lookup that never ran",
          "body": "The release tooling asked git questions and read the output without checking the command had run. Since git reports failures on its error stream, a lookup that failed left no output, and every gate read that as the permissive answer: an unreadable working tree as a clean one, an unreadable remote as a tag that had not been published. The clean-tree check runs immediately after the public tree is rebuilt, where \"clean\" means \"nothing to commit\" — so a failed lookup would have left that tree uncommitted and tagged the previous content. Each gate now stops and says which lookup failed, and a check refuses the shape so it cannot return."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "The vendored framework moves to 0.18.32",
          "body": "Not one line of the framework's runtime code changes across this bump — the release reworks how a blamejs release is produced, so that a publish which cannot find its packaged archive stops rather than publishing the working directory in its place. It is vendored because this server keeps its framework current as a release requirement, not because anything here needed it."
        }
      ]
    }
  ]
}
