{
  "version": "1.13.16",
  "date": "2026-07-25",
  "headline": "Keep uploaded content off disk and protect a restored backup",
  "summary": "Uploaded file content is now parsed entirely in memory, and a backup restore can no longer be reverted by the exit-time database re-encrypt.",
  "sections": [
    {
      "heading": "Security",
      "items": [
        {
          "title": "Refresh the vendored framework to 0.17.23",
          "body": "Update the vendored blamejs framework, picking up upstream session-security fixes — the session rotate and update-data paths now enforce the idle/absolute timeout floor (so a session past its idle timeout can no longer be revived by refreshing it), and a strict device-fingerprint policy with no request to compare against now fails closed — plus an audit-signature verifier that returns false on a malformed public key instead of throwing (hardening the backup-manifest verification the restore path relies on)."
        },
        {
          "title": "Parse uploads in memory so plaintext never lands on disk",
          "body": "File uploads are now parsed entirely in memory. Previously each uploaded part was streamed to a temporary file under the OS temp directory before HermitStash encrypted it, leaving unencrypted user content on the local filesystem for the duration of the request. The uploaded bytes now stay in RAM until they are sealed — closing that plaintext-at-rest window. Peak memory is unchanged; the upload pipeline already buffered the whole file."
        },
        {
          "title": "Harden path containment with the framework's audited primitive",
          "body": "Custom-logo serving and local storage-path resolution now confine request-derived paths through the vendored path-containment primitive instead of a hand-rolled prefix check. The primitive correctly rejects a sibling-directory prefix escape (for example a path resolving just outside the upload directory whose name shares the directory's prefix) that a bare string prefix comparison could miss."
        }
      ]
    },
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "A backup restore is no longer reverted by the exit-time database re-encrypt",
          "body": "Restoring from a backup writes a fresh encrypted database and key file, then restarts the server. The graceful-shutdown step re-encrypts the in-memory working database back to disk — which could overwrite the just-restored file with the stale pre-restore copy (under the previous key), silently reverting the restore or leaving the next boot unable to open the database. The restore now suppresses that exit-time re-encrypt so the restored data is what boots."
        },
        {
          "title": "Sealed-column queries and array updates stay correct",
          "body": "Paginated find and search now match a sealed column through its keyed blind index (and reject an unsupported operator on a sealed column loudly, instead of silently matching nothing). Appending an entry to a sealed array column now unseals the current array, appends, and reseals — preserving the existing entries and keeping the column encrypted rather than overwriting it in plaintext."
        }
      ]
    }
  ]
}
