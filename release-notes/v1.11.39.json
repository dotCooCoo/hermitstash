{
  "version": "1.11.39",
  "date": "2026-06-02",
  "headline": "Return the verification-flow errors as problem-details with extension members",
  "summary": "The last inline error responses that carry a client-read signal field — email-gate unlock, pending-account login, and passkey-removal confirmation — now use the RFC 9457 problem-details shape, with the signal field preserved as a top-level extension member.",
  "sections": [
    {
      "heading": "Changed",
      "items": [
        {
          "title": "Emit problem-details for the email-gate, pending-login, and passkey-confirmation responses",
          "body": "The email-verification-required response on stash and bundle unlock, the pending-account 403 on login and passkey login, and the vault-passkey-removal confirmation prompt now return application/problem+json with { type, title, status, detail } instead of the legacy { error } body. The fields the client reads to drive those flows — requiresEmail, pending, requiresConfirmation, email — are preserved as top-level problem extension members. AppError gained a withExtras() helper to attach them, and the error handler merges them into the problem document. This completes the problem-details migration for the API's error surface."
        },
        {
          "title": "Update the vendored blamejs framework to v0.14.19",
          "body": "Refreshes the bundled server framework and its SBOM-tracked nested dependencies to the current upstream release."
        }
      ]
    }
  ]
}
