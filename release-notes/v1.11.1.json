{
  "version": "1.11.1",
  "date": "2026-05-16",
  "headline": "Full test surface published + SMTP transport modernization",
  "summary": "The entire integration / security / performance / unit test suite (~18.8k LoC across 76 files) now ships in the repo tarball, outbound mail routes through `b.mail.transports.smtp`, vendored blamejs refreshes from v0.9.17 to v0.9.54, and `vault.init()` correctly awaits initialization before the first DB write under the sync fastpath. The `package.json` `version` field is corrected from a stale `3.x.y` typo to `1.11.1` to match `lib/constants.js`.",
  "sections": [
    {
      "heading": "Added",
      "items": [
        {
          "title": "Public test surface in the repo tarball",
          "body": "The full suite ships in tree so operators auditing the deployment can run it locally against a fresh checkout. Suite shape at this tag: Security 213 pass / 1 documented skip (CSRF policy, force-download `Content-Disposition`, rate-limit reset semantics, vault sealing of every protected field, zero-plaintext database verification, header hardening with `Referrer-Policy: no-referrer` + expanded `Permissions-Policy` deny set, passkey + WebAuthn flows); Performance 10 pass (template-render budget, 1000-record insert/query/cleanup, vault seal/unseal throughput, Argon2id hash/verify under 1s, concurrent upload behavior); Integration / E2E 422 pass with 4 documented failures tied to the Docker bind-mount sync gap on Windows Docker Desktop (host-to-container file events don't propagate through `b.watcher`'s `fs.watch`-only path; Linux and macOS hosts are unaffected)."
        },
        {
          "title": "SMTP transport via `b.mail.transports`",
          "body": "The hand-rolled SMTP state machine is gone; outbound mail now routes through `b.mail.transports.smtp` (with `b.mail.transports.resend` as the API-driven alternative). Same config keys, same boot-time validation; the transport itself is the framework primitive that backs the rest of the blamejs ecosystem and is exercised by upstream framework tests."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "Vendored blamejs refreshed v0.9.17 to v0.9.54",
          "body": "Brings in `b.cookies.parseSafe`, `b.safeJson.parseOrDefault`, `b.safeUrl.parse`, `b.safeAsync.sleep`, `b.problemDetails.send`, `b.middleware.composePipeline`, `b.testHarness.start`, `b.constants.TIME` / `BYTES`, and `b.auth.password.{hash,verify}`. Internal call sites have been migrated; no operator-visible API change beyond the wider RFC 9457 `application/problem+json` surface that started landing in v1.10.1."
        },
        {
          "title": "Internal refactors (no operator-visible change)",
          "body": "`b.middleware.composePipeline` adopted across the request-handling chain; raw time + byte literals (`* 1000`, `* 1024`) replaced with `b.constants.TIME` / `b.constants.BYTES`; `b.cookies` + `b.safeJson` + `b.safeUrl` sweep across cookie / JSON / URL parsing call sites; `b.safeAsync.sleep` replaces ad-hoc `setTimeout`-Promise wrapping; `b.problemDetails.send` adopted on additional error boundaries."
        }
      ]
    },
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "`vault.init()` sync-fastpath race",
          "body": "Boot path now correctly awaits vault initialization before the first DB write under the sync fastpath (was racing on cold start in v1.10.10). Symptom on the regression: `db.init() must be awaited` error emitted on the audit channel at boot, with the first write silently dropped. Fixed in tree as part of the v0.9.46 refresh."
        },
        {
          "title": "`package.json` `version` field typo",
          "body": "`package.json` `version` had been carrying a `3.x.y` typo since at least v1.10.8; corrected to `1.11.1` to match `lib/constants.js`. Test-suite stabilization on top of the v1.11.0 publish also lands here so operators running the suite locally see clean exit codes (security 213/0/1, performance 10/0)."
        }
      ]
    },
    {
      "heading": "Migration",
      "items": [
        {
          "title": "Drop-in upgrade",
          "body": "Pull the new image with `docker pull ghcr.io/dotcoocoo/hermitstash:1.11.1` or use the floating tags `1.11`, `1`, `latest`. No config migration required; no breaking changes to API, envelope, or database schema."
        }
      ]
    }
  ]
}
