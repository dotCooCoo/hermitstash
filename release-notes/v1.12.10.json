{
  "version": "1.12.10",
  "date": "2026-06-25",
  "headline": "Admin-list stored-XSS closure, checksum-verified restores, a durable mTLS lockout escape hatch, and a permanent backup/restore lock fix",
  "summary": "A self-registered user (or an invited address) can no longer plant script that runs in an administrator's session through the admin user and invite lists. Restores now verify every upload file against its recorded checksum and refuse a corrupt or tampered blob, the backup/restore operation lock can no longer latch permanently and block all future runs, and the mutual-TLS lockout escape hatch stays in effect across settings reloads instead of silently reverting. Backup blob storage, partial-restore handling, certificate rollback, webhook delivery, and several smaller correctness issues are also fixed.",
  "sections": [
    {
      "heading": "Security",
      "items": [
        { "title": "Stored cross-site-scripting closed in the admin user and invite lists", "body": "The admin user list and pending-invite list built their row action buttons by interpolating an account email directly into an inline event-handler's JavaScript string. The HTML escaper used there is correct for text and attribute context but not for a JavaScript string, so an email containing a quote — a value an anonymous self-registration or an invite can supply — broke out and executed in the administrator's authenticated session when a row action was clicked. The handlers now key off the server-generated record identifier and read the email from an escaped data attribute, so account-controlled text can never reach a script context." },
        { "title": "Restored upload files are checksum-verified", "body": "The restore path verifies each upload file against the SHA3-512 checksum recorded in the integrity-protected backup manifest before writing it, the same check the database and vault files already received. A corrupt, truncated, or tampered backup blob is now rejected instead of being written verbatim into live storage, and the failure is surfaced rather than counted as a successful restore." },
        { "title": "The mTLS lockout escape hatch survives settings reloads", "body": "Setting ENFORCE_MTLS_STRICT=false to recover access now disables app-layer mutual-TLS enforcement durably — it is re-asserted on every configuration reload, not only at boot. Previously the first settings save re-read the persisted database value and silently re-locked an operator who had used the escape hatch to regain access." },
        { "title": "Backup upload blobs are content-addressed", "body": "Backup upload blobs are stored under a content-addressed key rather than a single shared per-path key. A vault-password rotation rewrites a file's ciphertext in place; under the old shared key a later backup overwrote the blob an older backup still referenced, so restoring that older point-in-time produced undecryptable data. Content-addressing keeps each generation's blobs immutable while preserving cross-generation de-duplication." },
        { "title": "The API-encryption replay nonce covers the full freshness window", "body": "The single-use replay nonce now lives at least as long as the maximum message-freshness lifetime, closing a brief window in which a future-dated request timestamp let a captured ciphertext be replayed after the nonce expired but before the freshness check did. The two bounds are tied to one shared constant so they cannot drift apart." },
        { "title": "Freshness enforcement applies to every encrypted payload shape", "body": "The API-encryption timestamp freshness check is now enforced for every decryptable payload, not only those carrying the inner discriminator field. A request that omits it is treated as malformed and rejected when a replay window is configured." },
        { "title": "A toggled-off webhook stops delivering immediately", "body": "A webhook delivery that is in flight or awaiting a retry is now suppressed once an operator toggles the hook off, instead of completing to the endpoint the operator just disabled. The active state is re-checked at delivery time." }
      ]
    },
    {
      "heading": "Fixed",
      "items": [
        { "title": "Backup and restore no longer wedge permanently", "body": "A failure that occurred before the backup or restore worker started — a vault re-initialization in progress, a transiently unreadable database snapshot — left the operation lock held forever, so every subsequent backup and restore was refused as already in progress until the process was restarted. The lock is now released unconditionally on every exit path." },
        { "title": "A partial upload-restore failure is treated as a failure", "body": "When some upload files fail to restore, the restore is now rolled back and reported as a failure with the count surfaced to the operator and the audit log, rather than committing the restored database and posting success while a soft warning was silently dropped — which left the live database pointing at files that were never written." },
        { "title": "A failed restore reverts certificate material too", "body": "The restore rollback now snapshots and reverts the TLS and mutual-TLS certificate-authority files alongside the database and vault, so a restore that fails after the certificate step can no longer leave the trust chain at the backup's version while the database is reverted." },
        { "title": "Webhook event subscriptions from the admin UI deliver again", "body": "The admin webhook form offered event names the server never emits, so every webhook subscribed to a specific event was silently dead and only the all-events subscription worked. The form now lists the events the server actually fires, and an unknown event name is rejected when the webhook is created." },
        { "title": "Webhook delivery history is bounded", "body": "Webhook delivery records are now swept on a retention window instead of accumulating without limit, and the admin delivery view reads only the recent window through its timestamp index rather than loading a hook's entire history into memory to sort it." },
        { "title": "Out-of-range environment values report accurately", "body": "An out-of-range configuration value supplied through the environment no longer warns that a default will be used while the load path actually uses the invalid value as-is; the load path now applies the schema default so the diagnostic matches the server's behavior." }
      ]
    }
  ],
  "references": [
    { "label": "OWASP — Cross-Site Scripting", "url": "https://owasp.org/www-community/attacks/xss/" },
    { "label": "FIPS 202 (SHA-3 / SHAKE)", "url": "https://csrc.nist.gov/pubs/fips/202/final" }
  ]
}
