{
  "version": "1.14.0",
  "date": "2026-08-01",
  "headline": "Post-quantum mTLS certificates with automatic migration and broad security hardening",
  "summary": "The mTLS certificate authority moves to post-quantum ML-DSA-87, an existing classical certificate authority migrates itself automatically on a capable runtime, and a broad hardening of the upload, audit, authentication, and request-security surfaces lands alongside it.",
  "sections": [
    {
      "heading": "Added",
      "items": [
        { "title": "Migrate an existing classical sync CA to the post-quantum default automatically", "body": "On a runtime whose TLS stack can verify an ML-DSA certificate chain (OpenSSL 3.5 or newer), an existing ECDSA sync certificate authority migrates to ML-DSA-87 at startup. The rotation is crash-atomic — an interrupted migration rolls back to the previous, still-usable authority rather than stranding a broken CA. The superseded authority is trusted for a 30-day grace window so existing sync certificates keep connecting while their owners re-enroll; straggler certificates are revoked when the window closes. Set MTLS_CA_ALGORITHM=ECDSA-P384-SHA384 to keep the sync CA classical, or MTLS_AUTO_MIGRATE=false to defer the migration." }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        { "title": "Rebuild the mTLS certificate stack on the zero-dependency @blamejs/pki toolkit", "body": "Certificate-authority operations — CA generation, client-certificate signing, PKCS#12 packaging, and CRLs — now run on the vendored @blamejs/pki toolkit, and certificates are issued under ML-DSA-87 (FIPS 204) by default. Certificate-authority rotation, retention of a superseded root, and the runtime TLS-verification self-test run through the toolkit's own audited primitives." },
        { "title": "Split mTLS into a post-quantum sync CA and a classical browser CA", "body": "Sync and machine client certificates are signed by an ML-DSA-87 certificate authority, while the PKCS#12 certificates a person imports into a browser or OS keystore are signed by a separate classical ECDSA-P384-SHA384 authority — today's browsers cannot complete an ML-DSA client-auth handshake. The server trusts both, and each authority rotates and revokes independently." }
      ]
    },
    {
      "heading": "Removed",
      "items": [
        { "title": "Drop the @peculiar/x509 + pkijs certificate bundle", "body": "The previous pure-JS certificate bundle is no longer vendored; the single zero-dependency @blamejs/pki toolkit replaces it." }
      ]
    },
    {
      "heading": "Fixed",
      "items": [
        { "title": "Deliver the 413 response on an oversized encrypted request", "body": "An oversized request on an encrypted session now receives the Payload Too Large response instead of a connection reset." },
        { "title": "Keep the envelope migration from aborting on a stray value", "body": "A boot-time envelope migration no longer aborts when a non-sealed column holds a value that begins with the seal prefix." },
        { "title": "Fall back to the default on a negative numeric setting", "body": "A negative numeric configuration value — for example a mistyped maximum file size — falls back to its default instead of disabling the upload surface." },
        { "title": "Count access-code attempts atomically", "body": "Wrong access-code attempts are counted atomically, so the attempt cap can no longer be bypassed by parallel submissions." },
        { "title": "Guard download streams against a mid-stream error", "body": "A storage-backend error partway through a download no longer crashes the process." }
      ]
    },
    {
      "heading": "Security",
      "items": [
        { "title": "Enforce certificate binding and stash confinement on file and bundle mutation routes", "body": "Renaming or deleting a file or bundle with an API key now enforces the key's mTLS certificate binding and its stash and bundle confinement, so a bearer token presented without its client certificate can no longer reach the key owner's resources." },
        { "title": "Protect the shared sync bundle from anonymous overwrite", "body": "The shared bundle behind a public sync stash can no longer be overwritten by an unauthenticated request. The bundle is provisioned and bound to the stash when an operator issues the sync token, so an anonymous caller cannot claim or replace it." },
        { "title": "Bound chunked-upload reassembly memory", "body": "A completed chunked upload is capped before it is reassembled in memory, even under a No-limit policy, and concurrent reassemblies are bounded, so a synchronized burst of large uploads can no longer exhaust process memory." },
        { "title": "Serialize audit retention against encrypted archival", "body": "The tamper-evident audit chain's retention cleanup and encrypted archival can no longer interleave, which previously could leave the chain reporting a permanent false tamper alarm. The live chain is also periodically signed with a post-quantum checkpoint, and the archive signature covers the full manifest." },
        { "title": "Adopt replay-protected webhook signatures", "body": "Outbound webhooks are signed under the Standard Webhooks scheme — a versioned signature over the message id, timestamp, and body — giving receivers replay protection and algorithm agility." },
        { "title": "Sanitize operator-supplied legal-policy HTML", "body": "Privacy, terms, and cookie policy text is HTML-sanitized before it is rendered on the public policy pages." },
        { "title": "Close account-enumeration timing oracles", "body": "Password-reset, email-verification, and bundle access-code requests no longer wait on the outbound email before responding, so response timing no longer reveals whether an address exists." },
        { "title": "Harden the authentication surfaces", "body": "The soft mTLS gate validates a presented bearer token, passkey verification runs before any account-status check, and repeated second-factor failures now count against the pending sign-in rather than only per source address." },
        { "title": "Route secret-bearing JSON parsing through a non-leaking parser", "body": "Parsing of key material and other sensitive JSON no longer risks echoing input bytes into an error message." }
      ]
    },
    {
      "heading": "Migration",
      "items": [
        { "title": "Pin the classical CA for pre-OpenSSL-3.5 mTLS peers", "body": "A deployment whose mTLS peers cannot verify ML-DSA should set MTLS_CA_ALGORITHM=ECDSA-P384-SHA384 to keep the sync CA classical." },
        { "title": "Re-enroll sync clients after the CA migration", "body": "After the sync CA migrates to ML-DSA-87, existing sync clients re-enroll onto the new certificate authority within the 30-day grace window." }
      ]
    }
  ],
  "references": [
    { "label": "FIPS 204 (ML-DSA)", "url": "https://csrc.nist.gov/pubs/fips/204/final" },
    { "label": "Standard Webhooks", "url": "https://www.standardwebhooks.com/" }
  ]
}
