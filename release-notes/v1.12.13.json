{
  "version": "1.12.13",
  "date": "2026-06-26",
  "headline": "Second factor enforced on every sign-in path, safer key rotation and recovery, and tighter sync access control",
  "summary": "This release closes a set of authentication, concurrency, and key-handling weaknesses. Two-factor verification is now required on Google and passkey sign-in, not only password login, and browser sign-in has a working code-entry page to complete it. Vault key rotation, the database-key load path, and 2FA backup codes are hardened against races and mismatched-key data loss, and sync access control now disconnects revoked credentials, screens WebSocket connections against the IP blocklist and rate limits, and enforces the scope a key was issued with.",
  "sections": [
    {
      "heading": "Added",
      "items": [
        { "title": "Two-factor code-entry page for browser sign-in", "body": "Signing in with two-factor authentication enabled now presents a page to enter your authenticator code (or a backup code) and finish logging in. Previously the browser sign-in flow did not prompt for the code, so accounts with two-factor enabled could not complete a browser login." }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        { "title": "Scheduled backups honor the configured time of day", "body": "The daily backup now runs at the configured wall-clock time and time zone and keeps that anchor across restarts, instead of firing relative to process start and drifting after each restart." },
        { "title": "Automatic certificate renewal no longer locks out an offline sync client", "body": "When the server pre-stages a renewal for a client certificate nearing expiry, it leaves the current certificate valid until the client redeems the new one, and redeeming a renewal through enrollment now realigns the key's certificate binding. An offline or slow-to-renew client keeps working and can recover through its own renewal flow rather than being refused on every sync request until an operator intervenes." }
      ]
    },
    {
      "heading": "Fixed",
      "items": [
        { "title": "Large sync catch-up requests are paged", "body": "A sync client requesting its full change history no longer forces the server to load and decrypt an entire bundle at once; the change feed is read in indexed pages, bounding the work a single request can demand." },
        { "title": "Corrupted stored data fails with a clear error", "body": "A truncated or corrupted legacy encrypted blob now surfaces a typed decryption error instead of an uncaught range error, so a damaged file or partial restore fails cleanly." }
      ]
    },
    {
      "heading": "Security",
      "items": [
        { "title": "Two-factor authentication is enforced on Google and passkey sign-in", "body": "An account with TOTP enabled could complete a Google sign-in — and a passkey sign-in, which is verified without user verification and therefore proves possession only — without entering its second factor. Every sign-in path now passes through one enforcement point, so the second factor is required on Google, passkey, and password logins alike." },
        { "title": "Vault key rotation can no longer strand files under the discarded key", "body": "Rotating a vault's passkey could leave a concurrently uploaded file — or a file from an overlapping second rotation — encrypted under the old key while the account was switched to the new one, making that file permanently undecryptable. Vault rotation, upload, and delete are now serialized per account, rotation re-checks the file set and refuses to switch keys if anything is unaccounted for, and an overlapping rotation is rejected." },
        { "title": "A mismatched vault key no longer overwrites the database key", "body": "If the vault key was lost or replaced with a non-matching one while the encrypted database was intact, the server would regenerate and re-seal the database key before confirming it could read the database — destroying the only recoverable copy. The server now refuses to start in that situation and leaves the sealed database key untouched, so restoring the correct vault key recovers the data." },
        { "title": "A passkey assertion can be used for only one sign-in", "body": "A captured passkey assertion could be replayed in two simultaneous requests to mint two sessions, because the one-time challenge was only cleared in memory and not claimed atomically. The challenge is now claimed as a single-use token before verification, so a given assertion authenticates exactly once." },
        { "title": "Revoking a sync API key disconnects its live connection", "body": "Revoking a key closed it to new connections but left any already-open sync stream running, so a leaked key kept receiving the change feed after revocation. Revoking a key now closes its live sync connections, and the connection periodically re-checks that its key and account are still active." },
        { "title": "The sync WebSocket endpoint enforces the IP blocklist and rate limits", "body": "WebSocket sync connections bypassed the IP blocklist and per-IP rate limiting that the rest of the request pipeline applies, letting a blocked address still connect and leaving the credential lookup unthrottled. Both checks now run before any work on the connection." },
        { "title": "API-key scopes are enforced", "body": "Keys carried a labelled scope (such as upload or read) that mutating and read routes never checked, so a key issued for a narrow purpose acted with the full authority of the administrator who created it. Mutating routes now require the appropriate scope, an unused scope label was removed, and a key only carries an administrator's ownership override when the key itself was issued with admin scope." },
        { "title": "Session-key exchange requires a verified client certificate", "body": "The first-response key wrap for mTLS clients used the presented client certificate without confirming the certificate chain was verified, so a client offering an untrusted certificate could have the session key wrapped to a key it controls. The wrap now runs only for an authorized peer, matching the certificate checks used elsewhere." },
        { "title": "Uploaded files that cannot be content-checked are rejected", "body": "If the file-content and polyglot validation step raised an internal error, the upload was logged and then accepted anyway. That step now fails closed: an upload that cannot be validated is rejected and audited, and its reserved quota is released." },
        { "title": "Two-factor backup codes are reliably single-use", "body": "Submitting two different valid backup codes at the same time could restore one just-spent code to the live set. A backup code is now removed in a single row-level operation, so a consumed code cannot reappear." }
      ]
    }
  ],
  "references": [
    { "label": "CWE-367: Time-of-check Time-of-use (TOCTOU) Race Condition", "url": "https://cwe.mitre.org/data/definitions/367.html" },
    { "label": "NIST SP 800-63B — Authentication and Lifecycle Management", "url": "https://pages.nist.gov/800-63-3/sp800-63b.html" }
  ]
}
