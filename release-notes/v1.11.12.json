{
  "version": "1.11.12",
  "date": "2026-05-21",
  "headline": "Test-scoped codebase-patterns — sleep, hardcoded bind ports, DB isolation",
  "summary": "Adds three test-scoped antipatterns to the codebase-patterns catalog so the lint gate now scans `tests/*.test.js` and `tests/helpers/` in addition to `lib/`. The detectors catch race-prone shapes that historically slipped through review: fixed-duration sleeps that inflate the suite's wall-clock floor, hardcoded server bind ports that collide under parallel runs, and DB-handle instantiations missing the per-test isolation primitives.",
  "sections": [
    {
      "heading": "Added",
      "items": [
        {
          "title": "`_testFiles()` walker + scanScope routing",
          "body": "Adds the `tests/` walker that powers test-scoped antipatterns and a `scanScope: \"test\"` field on `KNOWN_ANTIPATTERNS` entries. The walker excludes `tests/node_modules/`, `tests/lint/` (the detector file itself), `tests/.test-output/`, and `tests/helpers/_*` (underscored infrastructure). Per-entry `requires:` companion regex satisfies the antipattern when the same file also matches a discipline-establishing shape (used by the DB-isolation detector)."
        },
        {
          "title": "`test-promise-settimeout-sleep` antipattern",
          "body": "Flags `await new Promise(r => setTimeout(r, N))` in test files. Fixed-duration sleeps race under parallel test runs and pad wall-clock floor. Replace with polling the actual condition (curl /health, check filesystem state, listen for child `exit` event). One legitimate offender is allowlisted with a documented reason: subprocess-exit wait in `vault-passphrase-server-boot.test.js` (the event being waited on IS the process death; polling cannot shortcut it)."
        },
        {
          "title": "`test-hardcoded-server-bind-port` antipattern",
          "body": "Flags `.listen(NNNN)` with a literal non-zero port in test files. Hardcoded bind ports collide under parallel runs when two suites pick the same value. Convention: `.listen(0)` + read `server.address().port` after bind. The existing HermitStash test helpers already follow this convention so the detector ships with an empty allowlist."
        },
        {
          "title": "`test-creates-db-handle-without-isolation` antipattern",
          "body": "Flags `b.db.create(` in test files when the same file does not also use one of the per-test isolation primitives (`setupTestEnv`, `startTestServer`, `setupTestDb`, `mkdtempSync`). Tests that spin up a real DB handle without isolation leak SQLite state to a shared directory; subsequent tests see prior rows under parallel runs. Static-API tests that reference `b.db` without instantiating a real handle don't trip the detector."
        }
      ]
    }
  ]
}
