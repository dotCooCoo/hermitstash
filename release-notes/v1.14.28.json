{
  "version": "1.14.28",
  "date": "2026-08-19",
  "headline": "Stop two-factor and passkey verification reporting which credential matched",
  "summary": "A constant-time comparison answers one question without leaking anything. A loop of them that stops at the first match answers a second question nobody asked: how far down the list the match was. Three verification paths did that, and each carried a comment asserting the property the early exit removed. Also here: turning public uploads off now stops a bundle staged beforehand from publishing, and the vendored framework moves forward four releases carrying its own fixes of the same shape.",
  "sections": [
    {
      "heading": "Security",
      "items": [
        {
          "title": "Two-factor backup codes are compared against every stored code",
          "body": "Redeeming a backup code compared the presented value against each stored hash and stopped at the first match. Each comparison was constant time, but the total time reported where in the list the match sat — and the list is ordered, so that narrows which of a user's backup codes was presented. Every stored code is now compared. The code is still consumed by position and the first match still wins; what changed is that the answer no longer arrives sooner for an earlier code."
        },
        {
          "title": "Passkey lookup no longer reports a credential's position",
          "body": "Resolving an assertion to a stored credential compared the presented credential id against each row and returned on the first hit, so response time reported the credential's position in the table — which is registration order. The comment above the comparison said it existed so timing could not enumerate which credential ids exist, which is what the early return gave back. Every row is now compared."
        },
        {
          "title": "The framework closes the same class in eight more places",
          "body": "blamejs 0.18.36 fixes multi-candidate comparisons that exited on the first match in certificate revocation checks, webhook signature verification, HTTP message signatures, attestation nonces, the DPoP nonce window, time-based one-time-password drift, and breached-password range checks. The one-time-password path is the one HermitStash uses: its drift loop reported the clock offset between authenticator and server through response time."
        },
        {
          "title": "Parsers that read untrusted bytes carry no pattern literals",
          "body": "blamejs 0.18.37 removes 55 regular expressions from the YAML, TOML, INI, .env and XML parsers, so a screen costs the length of its input and the byte cap that bounds the input bounds the screen. Behaviour is unchanged except that a YAML tag beginning with a digit is now refused, as the surrounding rule always described."
        },
        {
          "title": "Uploaded filenames drop four more invisible characters",
          "body": "A stored filename carrying U+2061 through U+2064 is stripped of it. U+2060 WORD JOINER was already removed and its four neighbours were not, so two names that render identically on screen could be stored as different bytes. Admin CSV exports drop DEL from a cell for the same reason, rather than emitting it."
        }
      ]
    },
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "Turning public uploads off stops a staged bundle publishing",
          "body": "Three of the four routes behind the public portal read the setting; the one that publishes did not. An operator switching the portal off to stop abuse could still have a bundle staged moments earlier go out — minting its share URL, mailing the uploader, notifying admins and firing webhooks. Nothing new can be staged while the switch is off, and a held bundle publishes normally once it is back on. Bundles owned by a signed-in user or a sync client are unaffected, since the setting governs anonymous uploads."
        },
        {
          "title": "Third-party licence notice names the real source of the framework",
          "body": "The notice described the vendored framework as a shallow git clone of a release tag. It is the published @blamejs/core package, whose bytes are checked against the registry's sha512 before anything is unpacked, and a version publishing no integrity value is refused rather than vendored."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "Vendored framework moves to blamejs 0.18.37",
          "body": "Four releases forward from 0.18.33. Beyond the fixes above: a guard now applies the character policy it declares rather than deciding by a finding's severity, the public-suffix list ranks a wildcard rule by label count as the specification requires, the session store releases its file handle when one store replaces another, and citations across the framework move to the documents that replaced RFC 7230-7235, 7489 and 4122."
        },
        {
          "title": "Runtime base image moves to the current build",
          "body": "The pinned Chainguard wolfi-base digest is refreshed. That image is rebuilt whenever upstream fixes land, so a pin left in place falls behind on patches without anything changing here."
        }
      ]
    }
  ],
  "references": [
    { "label": "CVE-2021-42574 — Trojan Source", "url": "https://nvd.nist.gov/vuln/detail/CVE-2021-42574" },
    { "label": "Unicode Standard Annex #31", "url": "https://www.unicode.org/reports/tr31/" },
    { "label": "RFC 6238 — TOTP", "url": "https://www.rfc-editor.org/rfc/rfc6238" }
  ]
}
