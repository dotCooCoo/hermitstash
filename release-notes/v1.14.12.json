{
  "version": "1.14.12",
  "date": "2026-08-10",
  "headline": "Refresh the vendored framework and the runtime base image",
  "summary": "The framework moves to 0.18.19. Nothing in this server's own code changes; the refresh brings a response-handling fix, a matcher that runs an operator's pattern without backtracking, and the current Chainguard base rebuild.",
  "sections": [
    {
      "heading": "Changed",
      "items": [
        {
          "title": "The vendored framework moves to 0.18.19",
          "body": "A request that failed after its first byte was already on the wire used to be finished off as though it had succeeded — the status line and headers cannot be replaced once sent, so an attempt to write a 500 threw and the handler fell through to appending an error string to a partial body. A client reading that sees a complete, successful response whose last line is odd. In one path the same attempt happened inside a promise rejection with nothing to catch it, which ended the process and every request in flight with it. Neither path could be reached here — this server registers its own error handler, and none of its routes write a response in parts — but the framework no longer has the hazard. The release also adds a matcher that runs a pattern without backtracking, so a pattern's cost is bounded by the length of what it is matched against rather than screened and hoped for, and the screen that remains no longer refuses the published semver and email patterns it used to."
        },
        {
          "title": "The runtime base image moves to the current Chainguard rebuild",
          "body": "The pinned digest had been superseded upstream. Bumping it picks up whatever package fixes landed in the rebuild."
        }
      ]
    }
  ]
}
