{
  "version": "1.14.20",
  "date": "2026-08-16",
  "headline": "Refuse database queries that cannot be answered, and report TLS from the listener rather than the files",
  "summary": "A query against an encrypted column used to return the wrong rows rather than an error, and the admin security panel could report protections the server was not applying. Both are corrected, and the vendored framework moves to 0.18.28.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "A query over an encrypted column no longer returns the wrong rows",
          "body": "Most columns are stored encrypted. A few also carry a searchable index, and an exact match or a list match against those can be answered. Anything else cannot: comparing a value against ciphertext produces a comparison that is quietly wrong rather than an error. A check for that existed but only covered the indexed columns, so a query against any other encrypted column was answered incorrectly and silently — an exact match found nothing, and a not-equal match returned every row including the one it was written to exclude. It now refuses, and says to decrypt and filter instead. Empty and blank values keep returning no rows, as they always did, so a submitted-but-empty search or sign-in field is unaffected. No existing screen or endpoint issued such a query, so nothing changes in day-to-day use."
        },
        {
          "title": "Admin → Security reported HTTPS and mutual TLS the server was not running",
          "body": "The panel decided whether HTTPS was on by looking for a private key. The server starts TLS only when the certificate is present as well, and otherwise runs plain HTTP — so a key installed ahead of its certificate, or a renewal that replaced one and not the other, showed a green HTTPS row on a server serving unencrypted traffic. The mutual-TLS row separately reported handshake-level enforcement, which is a property of the TLS listener, on servers that had no listener at all. Both now report what the server did when it started rather than re-reading the same files, and when enforcement is configured but cannot apply, the panel says which part is missing instead of repeating advice already followed."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "The vendored framework moves to 0.18.28",
          "body": "A row of an exported CSV that contains a disarmed spreadsheet formula is now written fully quoted, with the neutralising character inside the quotes where a spreadsheet treats it as literal text. Rows with nothing to disarm are unchanged. Exports were already neutralising every trigger, including one in the first column, so this is a stronger form of a protection that was working rather than a fix for a gap. The text screening applied to uploader-supplied names and messages is unchanged."
        }
      ]
    }
  ]
}
