{
  "version": "1.11.2",
  "date": "2026-05-19",
  "headline": "Fresh-install sync without a browser detour",
  "summary": "Sync clients can complete enrollment + initial-seed against a freshly-deployed server without an operator first clicking through the setup wizard in a browser. Two server-side fixes close the loop: `/sync/enroll` now returns `shareId` alongside `bundleId`, and the first-run setup middleware bypasses Bearer-authed requests instead of redirecting the sync API key to the wizard.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "`/sync/enroll` response carries `shareId`",
          "body": "The sync daemon needs `shareId` for the initial `GET /b/:shareId` seed pull. Without it, the WebSocket connection establishes but the local mirror starts empty and only catches files uploaded after connect. Resolved from the bundle row alongside the existing `bundleId` resolution from `stash.syncBundleId`. Consumed by `dotCooCoo/hermitstash-sync` v0.8.8+; older sync-client builds ignore the new field."
        },
        {
          "title": "First-run setup middleware bypasses Bearer-authed requests",
          "body": "A sync API key owned by the default admin previously tripped the wizard's `req.user.role === \"admin\"` 302 to `/admin/setup` until an operator completed the browser flow. Setup is a browser-only flow; Bearer-authed callers now pass through."
        },
        {
          "title": "Idempotency-Key body fingerprinting order",
          "body": "`middleware/idempotency.js` composes `b.middleware.bodyParser` before `b.middleware.idempotencyKey` so the body fingerprint populates before the cache lookup. Same key with a different body now correctly returns 422 `idempotency/key-reuse-mismatch` (RFC draft-ietf-httpapi-idempotency-key §4.3) instead of a wrong cache hit."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "Vendored blamejs 0.9.54 to 0.10.11",
          "body": "Five upstream patches forward; entirely additive primitives. Upgrade is drop-in. No config changes."
        }
      ]
    }
  ]
}
