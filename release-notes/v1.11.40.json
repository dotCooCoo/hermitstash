{
  "version": "1.11.40",
  "date": "2026-06-02",
  "headline": "Decrement customer-stash counters atomically",
  "summary": "Customer-stash bundle-count and byte-total decrements now run as a single atomic database update, so concurrent bundle deletions on the same stash can no longer lose a decrement.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "Decrement stash bundle counters in a single atomic update",
          "body": "Deleting a bundle decremented the owning stash's bundle count and byte total with a read-modify-write that could lose an update when two deletions on the same stash overlapped. Both decrements now run as one atomic UPDATE with a zero floor, keeping the displayed totals accurate under concurrency."
        }
      ]
    }
  ]
}
