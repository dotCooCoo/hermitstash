{
  "version": "1.11.16",
  "date": "2026-05-22",
  "headline": "`RP_ORIGIN` and other URL settings accept cleartext `http://` values again",
  "summary": "Restores `http://` support for `RP_ORIGIN`, `GOOGLE_CALLBACK_URL`, `S3_ENDPOINT`, and `BACKUP_S3_ENDPOINT`. Since v1.9.39 the settings validator silently rejected every cleartext URL and fell back to the code default, which made the server treat operator-set origins like `http://umbrel-dev.local:3080` as unconfigured — knock-on `Forbidden` responses from the origin-policy gate on every page load.",
  "sections": [
    {
      "heading": "Fixed",
      "items": [
        {
          "title": "`isValidUrl` opts into `b.safeUrl.ALLOW_HTTP_ALL`",
          "body": "`lib/settings-schema.js` now passes `allowedProtocols: b.safeUrl.ALLOW_HTTP_ALL` to `b.safeUrl.parse`, matching the validator's intent to accept both `http:` and `https:`. Previously the parse call relied on the safeUrl default (`ALLOW_HTTP_TLS = [\"https:\"]`), which threw `safe-url/protocol-disallowed` on every `http://` URL before the protocol check could run. Affected settings: `RP_ORIGIN`, `GOOGLE_CALLBACK_URL`, `S3_ENDPOINT`, `BACKUP_S3_ENDPOINT`. The `rpOrigin is not HTTPS` startup warning is unchanged — it remains the soft nudge toward TLS."
        },
        {
          "title": "`tests/unit/settings-schema.test.js`",
          "body": "New unit test asserts `settingsSchema.validate` accepts `http://` and `https://` for every url-typed setting, refuses non-http(s) schemes, refuses malformed input, and treats empty string as `use default`."
        }
      ]
    }
  ]
}
