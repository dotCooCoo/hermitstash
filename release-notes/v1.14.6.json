{
  "version": "1.14.6",
  "date": "2026-08-08",
  "headline": "Post-quantum key exchange is now actually enforced on incoming connections",
  "summary": "The server's post-quantum key-exchange preference was never reaching the TLS listener, so incoming connections were negotiating a classical key exchange. This release fixes that and raises the Node.js floor to 24.19.0. Operators running with post-quantum enforcement on should read the note below.",
  "sections": [
    {
      "heading": "Security",
      "items": [
        {
          "title": "The post-quantum group preference now reaches the TLS listener",
          "body": "The listener was configured with a `groups` option. Node has no such option — it is accepted and discarded without error — so the preference never applied and the listener fell back to OpenSSL's defaults, which exclude the ML-KEM hybrids. The effect was the opposite of the intent: a client offering only a post-quantum group was refused outright, and a client offering both a hybrid and a classical group negotiated the classical one. Incoming TLS connections were therefore classically keyed, and are not protected against store-now-decrypt-later attacks, even though the startup banner reported post-quantum as enforced. The preference is now supplied as `ecdhCurve`, which the runtime validates, so an incorrect value fails at startup instead of degrading silently. The ClientHello gate that refuses clients not offering a post-quantum group was working and is unchanged — it inspects what a client offers, which is why it could not detect what was actually negotiated."
        },
        {
          "title": "With enforcement on, the classical fallback is no longer offered",
          "body": "Key-exchange selection draws from the set both sides support, so leaving the classical last-resort in the list let a client that lists it first negotiate classical despite also offering a post-quantum group. With enforcement on (the default) the list is now post-quantum only, making the negotiated group post-quantum whenever the handshake completes. No client is newly refused: the ClientHello gate already rejects any client that does not offer a post-quantum group before the handshake begins. With enforcement disabled the full list, including the classical fallback, is unchanged."
        },
        {
          "title": "The startup banner reports the groups actually configured",
          "body": "Both startup lines named the framework's full preference list regardless of what the listener received, which is how the previous behaviour went unnoticed. They now report the list that was actually applied."
        }
      ]
    },
    {
      "heading": "Changed",
      "items": [
        {
          "title": "Raised the Node.js floor to 24.19.0",
          "body": "24.19.0 is the current long-term-support release; it bundles SQLite 3.53.3 and reports the negotiated TLS key-exchange group, which is what makes the post-quantum posture above verifiable rather than assumed. The boot check, the installer, the container image, and the continuous-integration matrix all move together."
        }
      ]
    }
  ]
}
